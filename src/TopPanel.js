import React from 'react';

const TopPanel = ({}) => {
  return (
    <div className='top-panel'>
      <button>Start</button>
      <button>Pause</button>
      <button>Reset</button>
    </div>
  );
};

export default TopPanel;
